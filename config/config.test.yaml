env: testing

serve:
  port: 8000
  setup: true

database:
  type: "sqlite"
  url: "file::memory:?cache=shared"

secret_key: "a-string-secret-at-least-256-bits-long"

email:
  host: "mail.google.com"
  port: 587
  username: "email@example.com"
  password: "password"

# token lifetimes in minutes (defaults: 15 / 43200)
auth:
  access_ttl: 15
  refresh_ttl: 43200
